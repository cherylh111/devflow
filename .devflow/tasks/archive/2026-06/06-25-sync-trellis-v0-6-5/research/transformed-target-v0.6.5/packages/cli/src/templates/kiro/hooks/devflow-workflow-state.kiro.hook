{
  "version": "1.0.0",
  "enabled": true,
  "name": "devflow-workflow-state",
  "description": "Inject DevFlow workflow state on each prompt",
  "when": {
    "type": "promptSubmit"
  },
  "then": {
    "type": "runCommand",
    "command": "{{PYTHON_CMD}} .kiro/hooks/inject-workflow-state.py",
    "timeout": 30
  }
}
